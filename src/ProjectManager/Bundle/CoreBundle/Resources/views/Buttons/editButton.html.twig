<a class="btn btn-primary pm_edit_btn {{ btn_classes|default("") }}"
        {{ object_url is defined ? 'href=' ~ object_url : '' }} {{ btn_id is defined ? 'id=' ~ btn_id : '' }}>
    <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
    {{ 'buttons.edit'|trans }}
</a>
