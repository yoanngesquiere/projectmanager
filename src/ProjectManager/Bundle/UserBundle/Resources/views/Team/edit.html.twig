{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Team edit : {{ team.name }}</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pm_user_team_list') }}">
            Back to the list
        </a>
    </li>
</ul>
{# Shows the team members #}
{% render(controller('ProjectManagerUserBundle:TeamMember:list', { 'teamId': team.id })) %}
{# Shows members that are not in the team yet #}
{% render(controller('ProjectManagerUserBundle:TeamMember:listNotInTeam', { 'teamId': team.id })) %}
{% endblock %}
